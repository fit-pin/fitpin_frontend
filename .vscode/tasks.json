{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"command": "gradlew clean",
			"label": "clean",
			"detail": "gradlew clean",
			"options": {
				"cwd": "${workspaceFolder}/android"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}